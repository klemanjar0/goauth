# Environment
ENV=development

# Database
DB_CONN="postgres://postgres:password@localhost:5432/goauth?sslmode=disable"

# HTTP Server
PORT=3000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=your_secret_access_key_min_32_chars
JWT_REFRESH_SECRET=your_secret_refresh_key_min_32_chars

# Application URL
APP_URL="http://localhost:3000"

# SMTP Configuration (for email verification)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_FROM="noreply@yourapp.com"
SMTP_PASSWORD="your_smtp_password"
SMTP_USER="your_smtp_user"

# Kafka Configuration
KAFKA_BROKERS=localhost:9092

# gRPC Server Configuration
GRPC_PORT=50051
GRPC_TLS_ENABLED=false
GRPC_CERT_FILE=/path/to/server.crt
GRPC_KEY_FILE=/path/to/server.key

# Notes:
# - For production, set ENV=production
# - ALWAYS use TLS in production (GRPC_TLS_ENABLED=true)
# - Generate strong JWT secrets using: openssl rand -base64 32
# - Generate TLS certificates using:
#   openssl req -x509 -newkey rsa:4096 -keyout server.key -out server.crt -days 365 -nodes
